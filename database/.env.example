# Example environment configuration for Supabase

# Project public URL (used by frontend/backend)
SUPABASE_PROJECT_URL=https://your-project.supabase.co

# Public anon key (used by frontend for safe queries)
SUPABASE_ANON_KEY=your-anon-key

# Database connection string (used only in backend)
SUPABASE_DB_URL=postgresql://postgres:<password>@db.<your-project>.supabase.co:5432/postgres

# Service role key (private, used only by server or scripts)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
